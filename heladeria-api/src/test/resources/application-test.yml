server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/heladeria_test
    username: postgres
    password: N_03012003
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: test-jwt-secret-1234567890abcdef1234567890abcdef
    expiration: 86400000

mercadopago:
  access-token: test-access-token
  notification-url:
